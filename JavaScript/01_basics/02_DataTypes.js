"use strict"; // use strict mode - treat all JS code as newer version

// alert(3 + 3) // we are using nodejs, not browser, so we can't use alert

let name = "Pragyesh"
let age = 22
let isLoggedIn = false
let state

// Date types and their range
// number => 2^53 - 1
// bigint => 2^53 - 1
// boolean => true/false
// null => standalone value, represents nothing
// undefined => standalone value, represents undefined value
// symbol => unique identifier

// object => collection of key-value pairs

console.log(typeof "pragyesh");
console.log(typeof age);
console.log(typeof null); //object
console.log(typeof undefined); //undefined